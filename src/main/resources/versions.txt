
Testsuite build at ${maven.build.timestamp}

${project.groupId}
${project.artifactId}
${project.version}

Branch: ${git.branch}
----------------------------------------------------------------------------------------------------

Java: ${java.version}

Tiger: ${tiger.version}

Cucumber-Test-Combinations-Plugin: ${cucumber-test-combinations-maven-plugin.version}

Serenity: ${serenity.core.version}

Ti-m-mtls: ${ti.m.mtls.certs.version}
